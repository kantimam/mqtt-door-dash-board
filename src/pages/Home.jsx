import { Box } from '@material-ui/core'
import React from 'react'

const Home = (props) => {
    console.log(props)
    return (
        <Box>
            home
        </Box>
    )
}

export default Home
